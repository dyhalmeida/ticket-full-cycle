export type TEvent = {
  id: string;
  name: string;
  organization: string;
  date: string;
  price: number;
  rating: string;
  image_url: string;
  location: string;
};

export type TSpot = {
  id: string;
  name: string;
  status: string;
};
